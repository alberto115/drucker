{
    "name": "abesiyo/drucker",
    "description": "Docker + drupal composer stack",
    "type": "project",
    "license": "GNU GPLv3",
    "minimum-stability": "dev",
    "config": {
      "platorm": {
        "php": "7.1"
      }
    },
    "authors": [
        {
            "name": "Alberto Contreras",
            "email": "abecontreras@me.com"
        }
    ],
    "repositories": [
        {
            "type": "composer",
            "url": "https://packages.drupal.org/8"
        }
    ],
    "require": {
        "composer/installers": "^1.2",
        "drupal-composer/drupal-scaffold": "^2.2",
        "cweagans/composer-patches": "~1.0",
        "drupal/core": "~8.2",
        "drush/drush": "~8.0",
        "drupal/console": "~1.0",
        "vlucas/phpdotenv": "^2.4"
    },
    "require-dev": {
    },
    "prefer-stable": true,
    "scripts": {
      "post-root-package-install": [
        "php -r \"file_exists('.env') || copy('.env.example', '.env');\""
      ],
      "post-create-project-cmd": [
        "php -r \"file_exists('src/sites/default/settings.local.php') || copy('example.settings.local.php', 'src/sites/default/settings.local.php');\"",
        "cd etc/nginx/ssl && openssl req -x509 -newkey rsa:4086 -subj \"/C=XX/ST=XXXX/L=XXXX/O=XXXX/CN=vm.localhost\" -keyout \"key.pem\" -out \"cert.pem\" -days 3650 -nodes -sha256"
      ],
			"post-install-cmd": [
      ],
      "post-update-cmd": [
			]
    },
    "extra": {
        "installer-paths": {
            "src/core": [
                "type:drupal-core"
            ],
            "src/modules/contrib/{$name}": [
                "type:drupal-module"
            ],
            "src/profiles/contrib/{$name}": [
                "type:drupal-profile"
            ],
            "src/themes/contrib/{$name}": [
                "type:drupal-theme"
            ],
            "drush/contrib/{$name}": [
                "type:drupal-drush"
            ]
        },
        "patches": { }
    }
}
